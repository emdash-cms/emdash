---
"@emdash-cms/admin": patch
---

Adds Hebrew (עברית) to the admin UI, with every string in the catalog translated: labels, descriptions, dialogs, form fields, the blocks and block-type screens, the capability consent dialog and the sign-in link pages. The locale is selectable from the language picker and renders right-to-left.
