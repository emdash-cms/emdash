---
"emdash": patch
"@emdash-cms/admin": patch
---

Fixes the admin plugin registry showing "Handle unavailable" for every publisher and scrolling sideways, so verified publisher handles appear again and unresolved publisher identifiers stay inside their cards.
