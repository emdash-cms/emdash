---
"emdash": patch
---

Fixes the image popover in Edit mode so alt text changes are saved and the media picker highlights the image currently in use. Previously the popover couldn't read the field's stored image, so alt text typed into it was discarded without an error.
