---
"emdash": minor
---

Removes the `emdash dev` command. Start sites with their existing package `dev` script so Astro uses the database configured for the project; run `emdash types` separately when manual type generation is needed.
