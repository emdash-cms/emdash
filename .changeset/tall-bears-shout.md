---
"emdash": patch
---

Fixes the visual editing toolbar so "Open in admin" for each entry targets its own browser tab, instead of reusing a single shared tab that clobbered previously opened entries.
