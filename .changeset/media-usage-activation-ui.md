---
"emdash": minor
"@emdash-cms/admin": minor
---

Adds a media usage tracking setting. The setup wizard selects it by default for new sites and lets administrators opt out before continuing. Existing sites enable it from Settings, keep the page open while EmDash scans existing content, and can return later to continue from saved progress.
