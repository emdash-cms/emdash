---
"@emdash-cms/registry-client": minor
---

Adds `compareVersions(a, b)` to `@emdash-cms/registry-client/env`. It returns the semver precedence of `a` relative to `b` as a negative number, zero, or a positive number, or `null` when either version is not valid semver.
