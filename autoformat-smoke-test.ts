// Throwaway file to smoke-test the auto-format pipeline. Delete with the PR.
export function add(a:number,b:number){
      return a+b
}

export const obj={x:1,   y:2,z:3}

export   const   greeting    =    "hello world"
