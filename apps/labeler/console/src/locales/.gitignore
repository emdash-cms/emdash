# Extracted and compiled Lingui catalogs — regenerated from source by
# console:build (extract + compile). English is the source locale with
# natural-language ids, so nothing hand-written lives here yet; translated
# catalogs get committed (and this ignore narrowed) when locales are added.
messages.po
messages.mjs

# Local backup files created by text editors or gettext tools
*.po~
