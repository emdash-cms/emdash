/**
 * In-memory atproto fakes for EmDash tests. PR 1 ships a stub; subsequent
 * commits in this slice land MockPds (real-crypto), MockJetstream,
 * MockDidResolver, and the createFakePublisher helper.
 */

export const __placeholder = true;
